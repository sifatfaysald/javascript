# জাভাস্ক্রিপ্ট টিপস অ্যান্ড ট্রিকস

কাজ করতে করতে যেসব ছোট ছোট জিনিস মনে রাখলে কোড ক্লিন থাকে এবং বাগ কম হয়, সেগুলো এখানে জড়ো করা হলো।

## ভ্যারিয়েবল ও তুলনা

- ডিফল্ট হিসেবে `const` ব্যবহার করুন, ভ্যালু পরে পাল্টাতে হবে জানলে তখনই `let` ব্যবহার করুন। `var` আর ব্যবহার করার দরকার নেই, এটা পুরনো এবং block scope মানে না।
- তুলনা করার সময় সবসময় `===` আর `!==` ব্যবহার করুন, `==` আর `!=` নয়। `==` টাইপ কনভার্ট করে তুলনা করে, যা মাঝেমধ্যে অপ্রত্যাশিত ফলাফল দেয় (যেমন `0 == "0"` কিন্তু `0 == ""` ও true)।
- `NaN === NaN` সবসময় `false`, তাই কোনো ভ্যালু NaN কিনা চেক করতে `Number.isNaN(value)` ব্যবহার করুন।

## ফাংশন ও অ্যারো ফাংশন

- object এর মেথড হিসেবে অ্যারো ফাংশন ব্যবহার করবেন না, কারণ অ্যারো ফাংশনের নিজস্ব `this` নেই। মেথডের জন্য সাধারণ ফাংশন বা shorthand method সিনট্যাক্স ব্যবহার করুন।
- একই কাজ করা ছোট ফাংশন বারবার না লিখে আলাদা ফাংশনে বের করে নিন, পরে পুরো কোডে রিইউজ করা যাবে।
- ফাংশনের নাম দেখেই যেন বোঝা যায় সেটা কী করে (যেমন `calculateTotal` এর বদলে শুধু `calc` না লেখা)।

## লুপের ভেতরে সাবধানতা

- `for` লুপে `var` ব্যবহার করলে setTimeout/callback এর ভেতরে সবসময় শেষ ভ্যালুটাই পাবেন, কারণ `var` block scoped না। `let` ব্যবহার করলে প্রতিটা ইটারেশনের নিজের কপি থাকে, এই সমস্যা হয় না।
- অ্যারে লুপ করার সময় সেই অ্যারের ভেতর থেকেই item push/splice করলে ইনফিনিট লুপ বা স্কিপ হওয়া আইটেমের সমস্যা হতে পারে। প্রয়োজনে আলাদা নতুন অ্যারেতে রেজাল্ট রাখুন।

## অ্যারে ও অবজেক্ট

- অ্যারে থেকে ডুপ্লিকেট বাদ দিতে চাইলে `[...new Set(array)]` একটা ছোট ও কার্যকর ট্রিক।
- অগভীর (shallow) কপি করতে spread ব্যবহার করুন: `const copy = { ...original }` বা `const copyArr = [...original]`। কিন্তু nested object/array থাকলে ভেতরেরটা এখনও reference-ই থেকে যাবে, দরকার হলে `structuredClone(original)` ব্যবহার করুন (গভীর কপির জন্য)।
- object এ কোনো key আছে কিনা চেক করতে `obj.key !== undefined` না লিখে `"key" in obj` অথবা `Object.hasOwn(obj, "key")` ব্যবহার করুন, কারণ ভ্যালু যদি ইচ্ছাকৃতভাবে `undefined` হয় তাহলে আগেরটা ভুল রেজাল্ট দিবে।

## Async কোড

- `.then().then().then()` চেইন বড় হয়ে গেলে পড়তে কষ্ট হয়, তখন `async/await` ব্যবহার করুন, পড়তে সহজ লাগবে।
- `async` ফাংশনের ভেতরে `await` লেখার সময় সবসময় `try/catch` দিয়ে ঘিরে রাখুন, নাহলে reject হলে কোথায় error গেলো বোঝা মুশকিল হয়ে যায়।
- একাধিক independent async কাজ একসাথে চালাতে চাইলে একটার পর একটা `await` না করে `Promise.all([...])` ব্যবহার করুন, অনেক দ্রুত হবে।

## ডিবাগিং

- শুধু `console.log` না দিয়ে `console.table(arrayOfObjects)` ব্যবহার করুন, অ্যারে অফ অবজেক্ট দেখতে অনেক পরিষ্কার লাগে।
- `console.log("value:", value)` এভাবে লেবেল সহ লগ করুন, শুধু `console.log(value)` দিলে পরে বোঝা কঠিন হয় এটা কী মাপার জন্য বসানো হয়েছিল।
- বার বার একই ভ্যারিয়েবল লগ করার বদলে `console.trace()` দিয়ে দেখুন ফাংশনটা কোথা থেকে কল হয়েছে।

## DOM নিয়ে কাজ করার সময়

- একই এলিমেন্ট বারবার `document.getElementById` দিয়ে খোঁজার বদলে একবার ভ্যারিয়েবলে রেখে দিন।
- অনেকগুলো ছোট এলিমেন্টের উপর আলাদা আলাদা event listener না বসিয়ে প্যারেন্ট এলিমেন্টে একটা listener বসিয়ে event delegation ব্যবহার করুন — পারফরম্যান্সেও ভালো, ডাইনামিকভাবে যোগ হওয়া এলিমেন্টের জন্যও কাজ করে।
- স্ক্রিপ্ট ট্যাগ HTML এর `<head>` তে দিলে `defer` অ্যাট্রিবিউট ব্যবহার করুন (`<script src="app.js" defer></script>`), তাহলে পুরো HTML পার্স হওয়া পর্যন্ত স্ক্রিপ্ট অপেক্ষা করবে এবং DOM এলিমেন্ট না পাওয়ার এরর হবে না। অন্যদিকে `async` স্ক্রিপ্ট যত দ্রুত সম্ভব ডাউনলোড হয়ে রান হয়ে যায়, HTML পার্সিং শেষ হওয়ার অপেক্ষা করে না — তাই DOM এর উপর নির্ভরশীল কোডের জন্য `async` উপযুক্ত না।

## সাধারণ ভুল যেগুলো এড়িয়ে চলা ভালো

- গ্লোবাল ভ্যারিয়েবল যতটা সম্ভব কম ব্যবহার করুন, ফাংশনের ভেতরে যা দরকার তা ফাংশনের ভেতরেই ডিক্লেয়ার করুন।
- ম্যাজিক নাম্বার (যেমন হঠাৎ কোথাও `86400` লেখা) সরাসরি না লিখে অর্থবহ নামের ভ্যারিয়েবলে রাখুন, যেমন `const secondsInADay = 86400`।
- try/catch দিয়ে error ধরে রেখে কিছুই না করা (silent fail) একটা সাধারণ ভুল, অন্তত `console.error` দিয়ে লগ করে রাখুন যাতে সমস্যাটা চোখের আড়ালে না যায়।
